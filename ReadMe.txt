Run the server, run the client.

Server will establish a socket and bind it to 5555 port. 

Client will connect to the server's socket and start throwing to this channel a "Ping" string. Server will respond with "Pong".

This "Ping"-"Pong" exchange will repeated 9 more times.